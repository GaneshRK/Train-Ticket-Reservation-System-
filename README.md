🚂 Train Ticket Reservation System
A straightforward console-based application implemented in C to manage train ticket reservations. The system utilizes a Linked List data structure to store and manage ticket details dynamically.

✨ Features
Booking Management: Allows creation and addition of new tickets to the system.
Unique ID Generation: Automatically assigns incremental Ticket IDs starting from 1001.
Cancellation: Tickets can be canceled (removed) from any position in the list using the unique Ticket ID.
Search Functionality: Quickly find a ticket by its ID.
Display & Count: View all current bookings or get a total count.

⚙️ Compilation and Execution
Prerequisites
A C compiler (e.g., GCC, Clang).
Compile
Save the code as train_reservation.c and use the following command to compile:
Bash
gcc -o train_reservation train_reservation.c
Run
Execute the compiled binary from your terminal:
Bash
./train_reservation
